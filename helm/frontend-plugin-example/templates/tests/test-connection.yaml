apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontend-plugin-example.fullname" . }}-test-connection"
  labels:
    {{- include "frontend-plugin-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontend-plugin-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
