apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend-plugin-example.fullname" . }}
  labels:
    {{- include "frontend-plugin-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "frontend-plugin-example.selectorLabels" . | nindent 4 }}
